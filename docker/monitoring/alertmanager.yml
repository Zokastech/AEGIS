# AEGIS — zokastech.fr — Apache 2.0 / MIT

global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Définir webhook Slack/PagerDuty via docker-compose.monitoring.yml (secrets).
