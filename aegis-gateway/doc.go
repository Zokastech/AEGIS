// AEGIS — zokastech.fr — Apache 2.0 / MIT
//
// Package gateway is the module root for the AEGIS HTTP gateway
// (github.com/zokastech/aegis/aegis-gateway). The runnable binary lives in
// ./cmd/aegis-gateway — use go build -o … ./cmd/aegis-gateway (not go build .).
package gateway
