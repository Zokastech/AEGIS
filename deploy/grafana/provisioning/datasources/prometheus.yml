# AEGIS — zokastech.fr — Apache 2.0 / MIT

apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus-aegis
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
