// AEGIS — zokastech.fr — Apache 2.0 / MIT

//! Stub for a future PyO3 integration in this crate.
//! The recommended Python SDK remains `sdk-python` (maturin + dedicated module).

/// Compile-time marker when the `python` feature enables `pyo3`.
pub const PYO3_ENABLED: bool = true;
